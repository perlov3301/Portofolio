document.addEventListener("DOMContentLoaded", (event) => {
    console.log("DOM fully loaded and parsed");
    var mydiv = document.getElementById("htmltest");
    // el.innerHTML = name;
    mydiv.innerHTML="Hello World";
});